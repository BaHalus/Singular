# Não responsabilidades — TraitFinalCost

`TraitFinalCost` não calcula grupos alternativos, não agrega o Character, não interpreta escolhas específicas, não escreve em `pointValue`, não altera importadores em calculadoras e não executa na UI.
