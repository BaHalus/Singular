# Invariantes — TraitChoices

- toda escolha possui `key` não vazia;
- chaves são únicas dentro do Trait;
- identidade não depende de nome, rótulo ou posição;
- valores são escalares ou `null`;
- somente escolhas `required` sem valor tornam a avaliação incompleta;
- escolhas não atribuem custo por conta própria.
