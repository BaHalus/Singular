{
  "name": "singular",
  "version": "0.1.0",
  "description": "Ficha GURPS 4e Mobile First",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "test": "sh -c 'node --test --test-reporter=dot > /tmp/singular-test.log 2>&1; status=$?; tail -n 80 /tmp/singular-test.log; exit $status'"
  }
}
